const ROOT_NAVIGATION = document.getElementById("navigation");
const ROOT_MAIN = document.getElementById("main");
const ROOT_MAP = document.getElementById("map");
const ROOT_TIMER = document.getElementById("timer");
